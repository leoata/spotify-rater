import React from 'react';
import {Typography} from "@mui/material";

const Loading = () => {
    return (
        <div className={"center"}><div><Typography variant={"h1"} component={"h1"}>Loading...</Typography></div></div>
    );
};

export default Loading;
